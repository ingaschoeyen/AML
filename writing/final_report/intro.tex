\section{Introduction}


\subsection{Related Work}

Search engines have mainly been implemented via retrieval algorithms that 


With the proliferation of large language models (LLM), the long-standing tradition of keyword-frequency-based retrieval algorithms is bei






\paragraph{Similar projects} 
Techniques from the domain of NLP, which range from statistical encoding techniques, to rule-based integration, to continuous-space representations (see \textcite{patilSurveyTextRepresentation2023} for a taxonomy), can be used to this end.

Search and retrieval systems have seen significant advancements, with the development of more sophisticated indexing and ranking algorithms, as well as the integration of NLP techniques to enhance search capabilities \cite{shalagin2024survey}.

Techniques, such as BM25 and TF-IDF, can be used to improve keyword-based search, while embedding-based approaches can be used to enable semantic search capabilities \cite{patilSurveyTextRepresentation2023}.


Two works that we highlight, the \texttt{BitAndBeam} software \cite{BitAndBeam} and the \texttt{SpaRAG} system \cite{gobels2024towards}, as prime examples of current state-of-the-art implementations of the required programme, the first a general-domain and the second a specialised system.
The \texttt{BitAndBeam} software \cite{BitAndBeam} 

the \texttt{SpaRAG} system \cite{gobels2024towards}



\subsection{Aim \& Approach}

% aim of the work
create a product that implements SOTA techniques in a value-driven manner, namely with respect to morality, safety, and sustainability.
Currently, we are witnessing the integration of LLMs into a steadily growing variety of applications, from ... to ...
meanwhile little oversight into safeguards + risk of hallucination make full automation unfeasible and undesireable 
the safety of employing them, particularly the privately owned and us-hosted models, which questionable labour practices, e.g. chatgpt and workers in kenya 
This can be largely circumvented by 

However, while llms can help with context understanding they require a lot of storage and energy 
Hybrid approaches offer to mitigate some of these concerns and offer robust performance for 
and so mapping out the golden point of contribution for hybrid approaches is 

hence, our focus lies on creating a product that avoids some of the pitfalls of the current trends in AI-supported product design, namely over-reliance on large models with insufficient oversight and consequently safety risks.

% approach
We set out to fulfill this aim by employing the following design principles: 

\begin{itemize}
    \item Ethical standards
    \item Sustainability: prioritise minimum-required 
    \item Safety: prioritise human control and oversight 
\end{itemize}

sustainability,

ethical practices/provides, modularity



% expected contribution
Here, we present a proof-of-concept implementation of our proposed system, with the methods we evaluated outlined in section \autoref{sec:methods}, and the results of the evaluation presented in section \autoref{sec:results}
We provide both a discussion of the performance and limitations of the implemented components, and suggestions for components that could be implemented in future implementations along with recommendations for doing this in a manner compliant with our design principles in section \autoref{sec:discussion}.